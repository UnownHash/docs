# Getting Started

Install Docker

Setup a database

Edit config files

docker-compose up -d

Modify your Pokemon Go Mitm to point to `http://192.168.1.XX:9002/`

## Updating

```sh
docker-compose pull
docker-compose up -d
```
